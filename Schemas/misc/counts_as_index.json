{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://dfc/schemas/misc/counts_as_index.json",
    "title": "DFC 'Counts As' Ships Index",
    "description": "The _counts_as.json index file for the 'counts as' ship entries extracted from the Misc PDF.",
    "type": "object",
    "required": [
        "source_version",
        "ships"
    ],
    "additionalProperties": false,
    "properties": {
        "source_version": {
            "description": "Release date of the source PDF (YYMMDD).",
            "type": "string",
            "pattern": "^\\d{6}$"
        },
        "ships": {
            "description": "Filenames of 'counts as' entry data files.",
            "type": "array",
            "items": { "type": "string" }
        }
    }
}
